import { StyleSheet } from "react-native";
import { colors } from '../../global/styles/colors';

export const styles = StyleSheet.create({
    card: {
        width: '43.7%',
        height: 150,
        marginHorizontal: 10,
        marginVertical: 10,
        borderColor: colors.types.grass,
        borderWidth: 2,
        borderRadius: 8,
    },

});
